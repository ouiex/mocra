# Revisions that `git blame` should skip (bulk reformats / mechanical rewrites).
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# (GitHub applies it automatically.)

# style: cargo fmt --all (workspace-wide, rustfmt 1.97) — pure formatting, no logic changes.
f99e89d9235a24031202b68091bd3c1e295476cf
